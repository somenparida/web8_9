PORT=5000
MONGODB_URI=your_mongodb_connection_string_here
JWT_SECRET=your_jwt_secret_here
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_app_password
NODE_ENV=production
ALLOWED_ORIGINS=https://your-frontend.onrender.com,http://localhost:3000

# NOTE: Do NOT commit real secrets. Use this file as an example and set the real
# environment variables in your deployment platform (Render) or a local .env
# which is ignored by git.
